apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend
  namespace: app
spec:
  ingressClassName: nginx

  tls:
  - hosts:
    - cashbot.funsdevops.com
    secretName: tls-cashbot

  rules:
  - host: cashbot.funsdevops.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 8080